# Copy to .env for local development.
# Note: the Go API currently only reads PORT, but we'll use the rest as we implement Milestone 1+.

# --- HTTP server ---
PORT=8080

# --- Database (for local tools / future local API runs) ---
# If you run the DB via docker-compose, this is the host connection string.
DATABASE_URL=postgres://eb:eb@localhost:5432/eastbay?sslmode=disable

# --- Auth (Milestone 1: JWT verification via JWKS) ---
# Required:
JWT_ISSUER=https://issuer.example.com/
JWT_AUDIENCE=east-bay-overland
JWT_JWKS_URL=https://issuer.example.com/.well-known/jwks.json

# Optional / legacy placeholders (may be removed once auth is implemented everywhere)
OIDC_ISSUER=http://localhost:5556

# --- Reverse proxy / base URL (used by docker-compose api today) ---
TRUST_PROXY_HEADERS=true
PUBLIC_BASE_URL=http://localhost:8081
